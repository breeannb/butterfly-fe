import React, { Component } from 'react'
import ListPage from './List.js'

export default class HomePage extends Component {
    render() {
        return (
            <div>
                {/* This is the home page. */}
                <ListPage />
                {/* Link to List Page? */}
                {/* Link to Details Page? */}
                {/* Link to Anything Else header/footer/etc? */}
            </div>
        )
    }
}
